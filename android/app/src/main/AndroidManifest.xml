<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 인터넷 권한은 이미 있습니다. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- FCM 및 로컬 알림에 필요한 권한 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Android 13 (API 33) 이상에서 알림을 표시하기 위한 권한 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:usesCleartextTraffic="true"
        android:label="connect"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true"> <!-- Android 13+ 뒤로가기 제스처 경고 해결 -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- FCM 메시지를 탭했을 때 앱을 열기 위한 인텐트 필터 -->
            <!-- 이 필터는 알림 클릭 시 앱의 특정 액티비티를 실행하도록 합니다. -->
            <intent-filter>
                <action android:name="FLUTTER_NOTIFICATION_CLICK" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- Flutter 툴이 플러그인 등록 코드를 생성하는 데 사용되는 메타데이터 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- FCM 알림 채널 설정 (필수) -->
        <!-- main.dart의 _showLocalNotification 함수에서 사용한 'high_importance_channel' ID와 일치해야 함 -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="high_importance_channel" />
        
        <!-- 기본 알림 아이콘 (선택 사항, 없으면 기본 앱 아이콘 사용) -->
        <!-- @mipmap/ic_launcher는 Android Studio 프로젝트 생성 시 기본으로 제공되는 아이콘입니다. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        
        <!-- 기본 알림 색상 (선택 사항, 없으면 시스템 기본 색상 사용) -->
        <!-- @color/ic_launcher_background는 res/values/colors.xml에 정의해야 합니다. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/ic_launcher_background" />

        <!-- FirebaseMessagingService 선언 (필수) -->
        <!-- 이 서비스는 FCM 메시지를 수신하고 처리합니다. -->
        <service
            android:name="com.google.firebase.messaging.FirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- flutter_local_notifications 백그라운드 작업 스케줄링을 위한 리시버 -->
        <!-- Android 12 (API 31) 이상에서는 exported="false"와 함께 명시적으로 선언해야 합니다. -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

    </application>
    <!-- Android 11 (API 30) 이상에서 패키지 가시성을 위한 쿼리 (kpostal 등에서 사용) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
